---
title: GET /v1/agents
description: List agents in the API key workspace.
---

# GET /v1/agents

List all agents that belong to the workspace attached to the API key.

## Authentication

Bearer API key required.

## Required scope

`agents:read`

## Query parameters

- `limit` - optional, defaults to `20`
- `cursor` - optional pagination cursor

## Response

```json
{
  "data": [
    {
      "_id": "agent_123",
      "workspaceId": "ws_123",
      "org_id": "org_123",
      "name": "Support Voice Agent",
      "description": "Answers customer support questions",
      "engine": "livekit",
      "type": "inbound",
      "instructions": "Answer product questions and route billing issues to support.",
      "sttProvider": "openai",
      "sttModel": "whisper-1",
      "llmProvider": "openai",
      "llmModel": "gpt-4o-mini",
      "ttsProvider": "openai",
      "voiceId": "alloy",
      "maxConcurrentCalls": 1,
      "system_prompt": "Answer product questions and route billing issues to support.",
      "greeting_message": "Hello, how can I help you today?",
      "voice": "alloy",
      "language": "en",
      "status": "active",
      "stt": {},
      "llm": {},
      "tts": {},
      "tool_ids": [],
      "knowledge_source_ids": [],
      "max_call_duration_seconds": 3600,
      "silence_timeout_seconds": 30,
      "interruption_handling": "queue",
      "metadata": {},
      "created_at": "2026-06-01T12:00:00.000Z",
      "updated_at": "2026-06-01T12:00:00.000Z"
    }
  ],
  "pagination": {
    "limit": 20,
    "cursor": null
  }
}
```

## Errors

```json
{
  "error": {
    "code": "UNAUTHORIZED",
    "message": "Invalid or missing API key.",
    "requestId": "req_abc123def456",
    "details": null
  }
}
```

```json
{
  "error": {
    "code": "FORBIDDEN",
    "message": "API key does not have access to this resource.",
    "requestId": "req_abc123def456",
    "details": null
  }
}
```

## cURL

```bash
curl https://api.elyntix.ai/v1/agents \
  -H "Authorization: Bearer elyntix_sk_live_xxxxx"
```

## JavaScript / TypeScript

```ts
const response = await fetch("https://api.elyntix.ai/v1/agents", {
  headers: {
    Authorization: `Bearer ${process.env.ELYNTIX_API_KEY}`,
  },
});

const payload = await response.json();
console.log(payload.data);
```
